# Synergy Lab - Telegram Bot Scheduler

**Лабораторная работа выполнена:** Тихонов Иван, студент группы ВБИо-402рсоб

## Описание проекта

Synergy Lab - это асинхронный Telegram бот, разработанный на Python с использованием aiogram, который позволяет запланировать и запустить пользовательские скрипты по расписанию. Бот поддерживает гибкую систему планирования с поддержкой крон-выражений (минуты, часы, дни, дни недели, месяцы).

## Основные функции

- 🤖 **Асинхронный Telegram бот** на базе aiogram
- ⏰ **Планировщик задач** с поддержкой гибкого расписания
- 📝 **Динамическая загрузка скриптов** из папки `scripts`
- 🔧 **Команды управления** для добавления и удаления задач
- 📊 **Обработка переменных окружения** через python-dotenv
- 🔄 **APScheduler** для управления расписанием

## Требования

- Python 3.10+
- pip (менеджер пакетов Python)

## Установка и запуск

### Шаг 1: Создание виртуального окружения

```bash
python -m venv venv

# На Windows:
venv\Scripts\activate

# На macOS/Linux:
source venv/bin/activate
```

### Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 3: Конфигурация переменных окружения

Создайте файл `.env` в корне проекта и добавьте:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
LOG_LEVEL=INFO
```

### Шаг 4: Добавление пользовательских скриптов

Поместите ваши Python скрипты в папку `scripts/`. Каждый скрипт **должен содержать функцию `main()`**, которая будет вызвана планировщиком:

```python
# scripts/my_script.py
def main():
    print("Скрипт выполняется!")
    # Ваш код здесь
```

### Шаг 5: Запуск бота

```bash
python main.py
```

## Использование

### Доступные команды

- `/start` - Начало работы с ботом
- `/help` - Справка по командам
- `/add_task` - Добавить новую задачу в планировщик
- `/remove_task` - Удалить задачу из планировщика
- `/list_tasks` - Показать список всех запланированных задач

### Добавление задач

#### Способ 1: Через команду в боте

Отправьте команду `/add_task` и следуйте инструкциям бота для:
- Выбора скрипта
- Установки расписания (крон-выражение)

#### Способ 2: Добавление в код

Прямое добавление в `main.py`:

```python
from apscheduler.schedulers.asyncio import AsyncIOScheduler

# Добавить задачу в планировщик
scheduler.add_job(
    func=my_function,
    trigger='cron',
    hour=10,
    minute=30,
    name='my_task'
)
```

## Структура проекта

```
synergy_lab/
├── main.py              # Основной файл приложения
├── config.py            # Конфигурация
├── func.py              # Вспомогательные функции
├── scheduler.py         # Логика планировщика
├── scripts/             # Папка с пользовательскими скриптами
├── src/                 # Исходные файлы
├── .env                 # Переменные окружения (не загружается в git)
├── .gitignore           # Правила игнорирования git
├── requirements.txt     # Зависимости проекта
└── README.md            # Этот файл
```

## Зависимости

| Пакет | Версия | Описание |
|-------|--------|----------|
| aiogram | 3.23.0 | Асинхронный фреймворк для Telegram Bot API |
| APScheduler | 3.11.1 | Планировщик задач |
| python-dotenv | 1.2.1 | Загрузка переменных окружения |

## Примеры использования

### Простой скрипт

```python
# scripts/hello_world.py
import logging

logger = logging.getLogger(__name__)

def main():
    logger.info("Hello from scheduled task!")
    print("Привет из планировщика!")
```

### Скрипт с параметрами

```python
# scripts/advanced_task.py
import asyncio
from datetime import datetime

def main():
    current_time = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    print(f"Задача выполнена в: {current_time}")
    # Ваша логика обработки данных
```

## Расписание (крон-выражения)

Примеры расписания для APScheduler:

- `hour=10, minute=30` - каждый день в 10:30
- `day_of_week='mon-fri', hour=9` - с Пн по Пт в 9:00
- `day=1, hour=0` - первого числа каждого месяца в 00:00
- `week='*', day_of_week='0', hour=12` - каждое воскресенье в 12:00

## Отладка

Установите уровень логирования в `.env`:

```env
LOG_LEVEL=DEBUG
```

Это включит подробное логирование всех операций бота.

## Проблемы и решения

### Бот не запускается

1. Проверьте, что `.env` файл создан и содержит `TELEGRAM_BOT_TOKEN`
2. Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
3. Проверьте версию Python: `python --version` (требуется 3.10+)

### Скрипты не выполняются

1. Убедитесь, что функция `main()` определена в каждом скрипте
2. Проверьте синтаксис крон-выражения расписания
3. Посмотрите логи для обнаружения ошибок

## Поддержка и контакты

В случае вопросов или проблем, пожалуйста, создайте Issue в репозитории.

## Лицензия

Этот проект создан в образовательных целях.

---

**Дата создания:** Декабрь 2025

**Автор:** Тихонов Иван (ВБИо-402рсоб)
